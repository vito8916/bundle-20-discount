query CartInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          product {
            bundleRole: metafield(namespace: "custom", key: "bundle_role") {
              value
            }
          }
        }
      }
    }
  }
}
